# tests/core_foundation/config/fixtures/env_prod.yaml
# 프로덕션 환경 설정

app:
  debug: false
  log_level: "WARNING"

gpu:
  memory_fraction: 0.9  # 프로덕션은 메모리 최대 활용
  batch_size: 16

camera:
  min_count: 4
  max_count: 8
