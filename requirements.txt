# COURTVIEW Desktop Requirements
# Python 3.10+

# Core
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# Computer Vision
opencv-python==4.9.0.80
opencv-contrib-python==4.9.0.80
ultralytics==8.1.15  # YOLOv8
mediapipe==0.10.9
pillow==10.2.0

# Deep Learning
torch==2.2.0
torchvision==0.17.0
onnxruntime-gpu==1.16.3  # GPU inference
numpy==1.26.3

# Database
sqlalchemy==2.0.25
alembic==1.13.1

# Video Processing
ffmpeg-python==0.2.0
imageio==2.33.1
imageio-ffmpeg>=0.6.0  # RTSP ffmpeg subprocess pipe (v0.2.0+) — 번들에 ffmpeg.exe 포함

# Network Discovery (Plan G, 2026-05-13) — mDNS / Zero-configuration networking
# 카메라 표준 서비스 광고 (_rtsp._tcp, _axis-video._tcp 등) 자동 발견.
# 미설치 시 mdns_discovery.py 가 graceful skip — 코드 동작에 영향 없음.
zeroconf>=0.130

# Multi-view
scipy==1.11.4
scikit-learn==1.4.0

# Cloud (v0.4.0 — S3 자동 업로드: 녹화 영상 + 데이터셋)
boto3>=1.34.0
zstandard>=0.22.0  # tar.zst / .jsonl.zst 압축 — 데이터셋 번들

# Utilities
python-dotenv==1.0.0
pyyaml==6.0.1
python-multipart==0.0.6
aiofiles==23.2.1
websockets==12.0

# Monitoring
loguru==0.7.2

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3

# Development
black==23.12.1
flake8==7.0.0
mypy==1.8.0
